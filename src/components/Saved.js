import React, { Component } from "react"
 
class Saved extends Component {
  constructor(props) {
    super(props)

    this.state = {
    }
  }

  render() {
    return (
      <React.Fragment>
        <h4>Saved</h4>
      </React.Fragment>
    );
  }
}
 
export default Saved;